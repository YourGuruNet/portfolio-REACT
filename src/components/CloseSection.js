import styled from "styled-components";

const CloseSection = styled.div`
  position: absolute;
  width: 100%;
  height: 100%;
  color: rgba(2, 2, 34, 0.7);
  align-items: center;
  z-index: 99;
  top: 0;
`;

export default CloseSection;
