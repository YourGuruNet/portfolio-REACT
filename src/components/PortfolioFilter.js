import React from "react";
import styled from "styled-components";

export default function PortfolioFilter() {
  return <FilterContainer>Hello from filter</FilterContainer>;
}

const FilterContainer = styled.div`
  margin-top: 2rem;
`;
